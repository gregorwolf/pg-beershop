sap.ui.define(['sap/fe/core/AppComponent'], function(AppComponent) {
    'use strict';

    return AppComponent.extend('Beers.display.Component', {
        metadata: {
            manifest: 'json'
        }
    });
});

